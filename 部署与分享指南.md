# 部署与分享指南

本项目是**纯静态前端**（Vite 打包成 `dist/`，没有后端、没有数据库），所以分享给别人非常简单：把 `dist/` 部署到任意静态托管，得到一个公网网址发给别人即可。

> 重要：本项目位于仓库的子目录 `第九特区/`。在各平台配置时，**根目录（Root Directory）请填 `第九特区`**。

项目用的是前端路由（`/map`、`/characters` 等真实路径），已经配好 **SPA 回退**（`public/_redirects`、`vercel.json`、`netlify.toml`），刷新或直接打开深链不会 404。

---

## 方式一：Vercel（最简单，推荐，免费）

1. 把代码推到 GitHub（见文末「推送到 GitHub」）。
2. 打开 https://vercel.com → 用 GitHub 登录 → New Project → 选择本仓库。
3. 配置：
   - **Root Directory**：`第九特区`
   - Framework Preset：Vite（一般自动识别）
   - Build Command：`npm run build`
   - Output Directory：`dist`
4. Deploy。完成后会得到一个 `https://xxx.vercel.app` 的网址，直接分享。

也可用命令行：
```bash
cd 第九特区
npx vercel        # 首次登录并部署（预览）
npx vercel --prod # 正式发布
```

## 方式二：Netlify（同样免费）

1. 代码推到 GitHub。
2. https://app.netlify.com → Add new site → Import from GitHub → 选仓库。
3. 配置：
   - **Base directory**：`第九特区`
   - Build command：`npm run build`
   - Publish directory：`第九特区/dist`
4. Deploy，得到 `https://xxx.netlify.app`。

命令行：
```bash
cd 第九特区
npm run build
npx netlify deploy --dir=dist --prod
```

## 方式三：Cloudflare Pages（免费，国内访问相对友好）

1. 代码推到 GitHub。
2. Cloudflare Dashboard → Workers & Pages → Create → Pages → 连接 GitHub 仓库。
3. 配置：
   - **Root directory**：`第九特区`
   - Build command：`npm run build`
   - Build output directory：`dist`
4. 部署后得到 `https://xxx.pages.dev`。

## 方式四：GitHub Pages（免费，但要改 base 路径）

GitHub 项目页面会把站点放在子路径 `/<仓库名>/` 下，需要给 Vite 设置 `base`：
```bash
cd 第九特区
npm run build -- --base=/<仓库名>/
```
然后把 `dist/` 发布到 `gh-pages` 分支（可用 `npx gh-pages -d dist`）。
注意：GitHub Pages 没有服务端回退，深链刷新可能 404，常见做法是把 `dist/index.html` 复制一份为 `dist/404.html`。
如果用 `<用户名>.github.io` 这种用户主页仓库，则 base 用默认 `/` 即可，最省事。

---

## 临时快速分享（不想部署，只想现在发给别人看一眼）

在你自己电脑上跑，然后用隧道工具映射成公网地址（电脑关机或断网后链接失效）：
```bash
cd 第九特区
npm run build
npm run preview        # 本地 http://127.0.0.1:4173

# 另开一个终端，二选一：
npx cloudflared tunnel --url http://127.0.0.1:4173   # 输出一个临时公网网址
# 或
npx localtunnel --port 4173
```
把输出的临时网址发给别人即可。适合演示，不适合长期分享。

---

## 推送到 GitHub（前三种方式的前提）

当前代码在本地分支 `feature/ninth-district-map`。
```bash
cd 第九特区
# 1) 在 GitHub 新建一个空仓库（不要勾选 README）
# 2) 关联并推送：
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin feature/ninth-district-map
# （也可以先把该分支合并到 main 再推 main）
```

---

## 说明

- 部署产物完全自包含：人物/场景图在 `public/`，章节索引已生成为 `src/data/chapterIndex.generated.json` 并打包进 `dist`，**不需要**原始小说 txt（它已被 `.gitignore` 忽略，也不参与构建）。
- 之后新增/替换图片：把 `<id>.png` 放进 `public/portraits/` 或 `public/scenes/`，重新构建部署即可生效。
- 全部为静态资源，无需服务器、无隐私后端。
